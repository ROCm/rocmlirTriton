# TODO(rocmlirTriton): error: 'rock.gemm' op operand #2 must be tensor of f8E8M0FNU type values, but got 'tensor<1x512x1xf32>'
config.unsupported = True

if not (hasattr(config, 'arch') and config.arch and "gfx950" in config.arch):
    config.unsupported = True
