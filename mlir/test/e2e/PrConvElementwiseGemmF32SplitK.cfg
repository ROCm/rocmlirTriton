# TODO(rocmlirTriton): error: 'rock.transform' op input shape must match transform lower bounds (input shape=...)
config.unsupported = True

if (not config.arch_support_mfma) or (not 'atomic_add' in config.features):
  config.unsupported = True
