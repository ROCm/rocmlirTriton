# TODO(rocmlirTriton): error: 'tosa.conv2d' op expected input_height - 1 + pad_top + pad_bottom - (kernel_height - 1) * dilation_y to be wholly divisible by stride_y, got (28 - 1 + 1 + 1 - (3 - 1) * 1) / 2
config.unsupported = True

if (not config.arch_support_mfma) or (not 'atomic_add' in config.features):
  config.unsupported = True
