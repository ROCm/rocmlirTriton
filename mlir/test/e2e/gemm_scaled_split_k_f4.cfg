# TODO(rocmlirTriton): Disable for now, fix later.
# rocmlirTriton/mlir/lib/Dialect/Rock/IR/TransformMapBuilder.cpp:835: void mlir::rock::BottomUpTMBuilder::unmerge(ArrayRef<StringRef>, ArrayRef<uint32_t>, StringRef, ArrayRef<int64_t>): Assertion `lengthsProd == totalLength && "failed to partition unmerge length among upper dimensions"' failed.
config.unsupported = True

if not (hasattr(config, 'arch') and config.arch and "gfx950" in config.arch):
    config.unsupported = True
