# TODO(rocmlirTriton): PrConvElementwiseGemmBF16SplitK/pr_conv_gemm_bf16_splitk_1.mlir fails with [0 1 1] on gfx950
config.unsupported = True

if (not config.arch_support_mfma and not config.arch_support_wmma) or (not 'atomic_add_bf16' in config.features):
  config.unsupported = True
