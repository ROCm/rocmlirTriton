# TODO(rocmlirTriton): LLVM ERROR: Unsupported DotScaleOp found when converting TritonGPU to LLVM.
config.unsupported = True

if not (hasattr(config, 'arch') and config.arch and "gfx950" in config.arch):
    config.unsupported = True
